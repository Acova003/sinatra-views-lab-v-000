<!DOCTYPE html>
<html>
  <body>
    <h1>Today</h1>
    <% day = Date.today.strftime("%A") %>
    <% month_num = Date.today.strftime("%B %d")%>
    <% year = Date.today.strftime("%Y")%>
    <%= "The current date is #{day}, #{month_num}, #{year}" %>
  </body>
</html>